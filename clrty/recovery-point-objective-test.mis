// Moniversive Invariant Static (MIS) — deep root
// Creator: Chandler William Ferguson
// Letter-hash + EMBED gates 3..=6 · settlement clrty-1 / chain 1202
// Compile: bin/misc <file>.mis --check --compact-letters
// Title: Recovery Point Objective Test · public entry

module RecoveryPointObjectiveTestEntry {

  invariant letter_hash_bound: letter_hash_root != @0;
  invariant deep_root_moniversive: deep_root == moniversive;
  invariant settlement_chain: chain_id == 1202;
  invariant settlement_network_clrty1: settlement_network == clrty_1;
  invariant kernel_is_misc: compiler_kernel == misc;
  invariant domain_bound: domain == recovery_point_objective_test;
  invariant github_repo_bound: github_repo_bound == true;

  outcome bind_kernel() {
    constraint kernel_module_bound == true;
    constraint compiler_kernel == misc;
  }

  outcome pack_dispatch(pack_id: Bytes, payload: Bytes) {
    require pack_id != @0;
    require payload != @0;
    constraint pack_executed == true;
  }

  outcome settle_clrty1(intent: Bytes) {
    require intent != @0;
    constraint chain_id == 1202;
    constraint settlement_network == clrty_1;
  }

  outcome bind_github_repo() {
    constraint github_repo_bound == true;
    constraint chain_id == 1202;
  }
}
